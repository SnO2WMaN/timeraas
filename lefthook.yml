pre-commit:
  parallel: true
  commands:
    eslint:
      glob: "*.{js,jsx,ts,tsx}"
      run: eslint --fix {staged_files}
    prettier:
      glob: "*.{json,yml,graphql}"
      run: prettier --check --write {staged_files}
